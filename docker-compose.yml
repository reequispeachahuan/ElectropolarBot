services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: solarbot
      POSTGRES_USER: solarbot
      POSTGRES_PASSWORD: solarbot
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data:
